using UnityEngine;
using System.Collections;

public class TextInlineSpriteDemo : MonoBehaviour {
    public InlineSpriteManager lsManager;
	// Use this for initialization
	void Start () {
        lsManager.SaveTextfor();
    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
